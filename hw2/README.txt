執行方法：
在matlab中打開含有 資料夾「pic」 以及 hw2.m 的資料夾，接著在 command window 打 “hw2” ，或是直接按下執行鍵即可執行。command window上會顯示a b 小題生成的圖片與原圖之間的PSNR值，在資料夾「pic」下會存下對應的圖片。

注意：
如果 “orig = imread('./pic/柴犬飛飛.jpg');” 若裡面中文字是亂碼表示編碼不同（mac下matlab打開沒問題，windows不知道），請在執行前換成上述的執行指令。